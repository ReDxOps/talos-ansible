---

services:
  app:
    image: "wolveix/satisfactory-server:{{ satisfactory_image_tag }}"
    container_name: "{{ satisfactory_role_name }}"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    volumes:
      - ./data:/config
    ports:
      - "{{ satisfactory_external_game_port | string }}:{{ satisfactory_game_port | string }}/udp"
      - "{{ satisfactory_external_game_port | string }}:{{ satisfactory_game_port | string }}/tcp"
      - "{{ satisfactory_external_messaging_port | string }}:{{ satisfactory_messaging_port | string }}/tcp"
    deploy:
      resources:
        limits:
          memory: "{{ satisfactory_memory_limit }}"
        reservations:
          memory: "{{ satisfactory_memory_reservation }}"
    environment:
      MAXPLAYERS: "{{ satisfactory_server_maxplayers }}"
      SERVERGAMEPORT: "{{ satisfactory_game_port }}"
      SERVERMESSAGINGPORT: "{{ satisfactory_messaging_port }}"
