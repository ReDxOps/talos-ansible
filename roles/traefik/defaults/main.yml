---
traefik_stack_state: "present" # present | stopped | absent
traefik_acme_email: "remydubois411@gmail.com"
traefik_app_image: ""
traefik_app_image_tag: ""
traefik_dashboard_auth: "{{ vault_traefik_dashboard_auth }}"
traefik_log_level: "INFO"
traefik_role_name: "traefik"
traefik_root_dir: "{{ project_root_dir }}/{{ traefik_role_name }}"
traefik_reverse_proxy_network: "{{ traefik_network }}"
traefik_web_url: "{{ traefik_role_name }}.{{ traefik_domain }}"
traefik_http_port: "{{ http_port }}"
traefik_https_port: "{{ https_port }}"
